{% extends "base.html" %}
{% block content %}
<div class="wispy-frame">
  <div class="mb-4">
    <h3>Auto-Recharge Settings</h3>
    <p class="text-muted">Manage automatic credit purchases when your balance runs low.</p>
  </div>

  <div class="card border-0 shadow-sm mb-3">
    <div class="card-body">
      <form method="POST" action="{{ url_for('auto_recharge_settings') }}">
        <div class="mb-3">
          <div class="form-check form-switch">
            <input class="form-check-input" type="checkbox" id="enabled" name="enabled"
                   {% if settings.enabled %}checked{% endif %}>
            <label class="form-check-label" for="enabled">
              <strong>Enable Auto-Recharge</strong>
            </label>
          </div>
          <small class="text-muted">
            When enabled, we'll automatically purchase credits when your balance reaches 0.
          </small>
        </div>

        <div class="mb-3">
          <label for="amount" class="form-label"><strong>Recharge Amount</strong></label>
          <select class="form-select" id="amount" name="amount">
            <option value="500" {% if settings.amount == 500 %}selected{% endif %}>
              $5.00 (250 brushstrokes)
            </option>
            <option value="1000" {% if settings.amount == 1000 %}selected{% endif %}>
              $10.00 (500 brushstrokes)
            </option>
            <option value="2000" {% if settings.amount == 2000 %}selected{% endif %}>
              $20.00 (1,000 brushstrokes)
            </option>
          </select>
          <small class="text-muted">
            Choose how much to charge when auto-recharge is triggered. ($0.02 per brushstroke)
          </small>
        </div>

        <div class="alert alert-info">
          <strong>Note:</strong> You must have a valid payment method on file for auto-recharge to work.
          You can manage your payment methods in the <a href="{{ url_for('portal') }}">Customer Portal</a>.
        </div>

        <div class="d-flex gap-2">
          <button type="submit" class="btn btn-wispy">Save Settings</button>
          <a href="{{ url_for('dashboard') }}" class="btn btn-outline-secondary">Cancel</a>
        </div>
      </form>
    </div>
  </div>

  <div class="card border-0 shadow-sm">
    <div class="card-body">
      <h5 class="fw-bold mb-3">How Auto-Recharge Works</h5>
      <ol>
        <li class="mb-2">When your brushstroke balance reaches 0, we'll automatically attempt to charge your saved payment method.</li>
        <li class="mb-2">The charge will be for the amount you selected above.</li>
        <li class="mb-2">Once the payment succeeds, credits will be immediately added to your account.</li>
        <li class="mb-2">You can disable auto-recharge at any time, or change the recharge amount.</li>
      </ol>
      <p class="text-muted mb-0">
        <small><strong>Tip:</strong> Auto-recharge is enabled by default for your convenience, but you can always turn it off if you prefer to manually purchase credits.</small>
      </p>
    </div>
  </div>
</div>
{% endblock %}
